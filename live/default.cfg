module.exports = {
	"HOST":		"127.0.0.1",
	"PORT":		80,
	"PROTOCOL":	"http",
	"DOC_ROOT":	".",
	"INDEX":	"index.html, index.php, README.txt, README.md, default.html, index.htm",
	"ON_404_TRY_INDEX":	true, // or a specific filename to override the 'INDEX' list

	// This is pointless for others than 'debug', but noone will care for the time being, right?
	// (Espec. as vastly more stupid things can be found here anyway...)
	"LOG_FILTER":	['debug',/*'notice'*/], // block some noise (debug, notice, warning, error)
}
